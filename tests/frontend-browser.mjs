// Run with: NODE_PATH=<playwright installation>/node_modules node tests/frontend-browser.mjs
// Browser actions against real server; no fixture rows or mocked API responses.
import {createRequire} from 'node:module';import assert from 'node:assert/strict';
const require=createRequire(import.meta.url);const {chromium}=require('playwright');const browser=await chromium.launch({executablePath:process.env.CHROMIUM_EXECUTABLE_PATH||undefined,headless:true,args:['--no-sandbox']});const page=await browser.newPage();const errors=[];page.on('pageerror',e=>errors.push(e.message));
try{await page.goto(process.env.FRONTEND_URL||'http://127.0.0.1:8765');await page.getByRole('heading',{name:'FOLLOW THE WALLETS. SEE THE CONNECTIONS.'}).waitFor();await page.getByRole('button',{name:'Method',exact:true}).click();await page.getByText('Rules, not predictions.').waitFor();await page.getByRole('button',{name:'Traders',exact:true}).click();await page.getByRole('heading',{name:'Observed wallets',exact:true}).waitFor();await page.getByRole('button',{name:'Radar',exact:true}).click();await page.getByRole('button',{name:'Exits',exact:true}).click();await page.getByText('Observed sells only').waitFor();await page.getByPlaceholder('Token address, symbol or name').fill('<script>');await page.getByRole('button',{name:'Inspect token'}).click();await page.getByText('Enter a valid Solana token address to inspect.').waitFor();await page.setViewportSize({width:390,height:844});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);assert.deepEqual(errors,[]);console.log('PASS: navigation, trade filter, address validation, mobile overflow, JS errors');}finally{await browser.close();}
